/* import { useEffect, useState } from "react";

export default function useSelection(cell) {
  const [cells, setCells] = useState([]);
  useEffect(() => {
    function handleCellSelection(cell) {
      setCells([...cells, cell]);
    }
    handleCellSelection(cell);
  });
  return [cells, setCells];
}
 */
