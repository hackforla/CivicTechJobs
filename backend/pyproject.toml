[tool.poetry]
name = "backend"
version = "0.1.0"
description = ""
authors = ["Aveline-art <62368440+Aveline-art@users.noreply.github.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
Django = "^4.2.1"
djangorestframework = "^3.14.0"
psycopg2-binary = "^2.9.6"
pyyaml = "^6.0"
uritemplate = "^4.1.1"

[tool.poetry.group.staging]
optional = true

[tool.poetry.group.staging.dependencies]
gunicorn = "^20.1.0"
whitenoise = "^6.4.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
